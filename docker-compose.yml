services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: taskapi
      POSTGRES_PASSWORD: taskapi
      POSTGRES_DB: taskapi
    ports:
      - "5432:5432"
    volumes:
      - taskapi-pgdata:/var/lib/postgresql/data

volumes:
  taskapi-pgdata:
